// C++ code
//



 
  
void setup()
{
  /*
  int temperatura = 50;
   Serial.begin (9600);

  pinMode(13, OUTPUT);
  
  if (temperatura >=30){
  digitalWrite(13, HIGH);*/
    
    
 /*
 
 pinMode(13, OUTPUT);
  Serial.begin (9600);
  if(porcentagem>=81){
   digitalWrite(13, LOW);
  }
  if(porcentagem<=80 & porcentagem >=20);{
  digitalWrite(13,HIGH);
  }
 if (porcentagem <=19){
 digitalWrite(13,LOW);   
    */
  
  /*
   int resultado= 1000;
int valor=1023;
int limite=10;
  
  
  pinMode(alerta,OUTPUT);
  Serial.begin(9600);
  resultado= map (valor,0,1023,0,100);
    limite=constrain(resultado,20,80);
  if (resultado >=20&&resultado<=80);
   
  {digitalWrite(alerta,HIGH);
    Serial.println("O valor de ");
  Serial.print(resultado);
  Serial.println(" nao excede o limite");
  */
  
  /*  int numero = 40
   int resto = %2
   if (resto == 0){
 Serial.println("numero par");
 }
  else{Serial.println("numero impar");
 */ 
    
    
  
  } 
     


void loop()
{
  
  
  
  

  
}